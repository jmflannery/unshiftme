<% if @message_routes %>
  <% @message_routes.each do |message_route| %>
    $('<%= "##{message_route.workstation.abrev}.recipient_workstation.other.on" %>').turnOff().removeData('recipient_id');
  <% end %>
<% end %>
