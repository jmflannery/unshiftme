<% if @user.desks.include?(recipient_desk.id) %>
  <div id='<%= recipient_desk.abrev %>' class='recipient_desk mine'>
<% elsif @user.messaging?(recipient_desk.id) %>
  <div id='<%= recipient_desk.abrev %>' class='recipient_desk on <%= @user.recipient_id(recipient_desk.id) %>'>
<% elsif %>
  <div id='<%= recipient_desk.abrev %>' class='recipient_desk off'>
<% end %>
  <div class='<%= "recipient_desk_id #{recipient_desk.id}" %>'>
    <%= recipient_desk.name %>
  </div>
  <% if recipient_desk.user_id and recipient_desk.user_id > 0 %> 
    <div class='<%= "recipient_user_id #{recipient_desk.user_id}" %>'>
      <%= "(#{User.find_by_id(recipient_desk.user_id).user_name})" %>
    </div>
  <% else %>
    <div class='recipient_user_id'>
      (vacant)
    </div>
  <% end %>
</div>
