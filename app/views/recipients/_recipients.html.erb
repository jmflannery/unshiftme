<% count = 0 %>
<% size = recipients.size %>
<% unless recipients.empty? %>
  <p>Messaging: 
  <% recipients.each do |recipient| %>
    <% if count == (size - 1) and size > 1 %>
      <span>and</span>
    <% elsif count > 0 %>
      <span>,</span>
    <% end %>
    <% name = User.find(recipient.recipient_user_id).name %>
    <% options = { method: :delete, remote: true, format: :js, class: "recipient_user" } %>
    <%= link_to(name, recipient, options) %>
    <% count += 1 %>
  <% end %>
  </p>
<% else %>
  <p>Messaging Nobody.</p>
<% end %>
