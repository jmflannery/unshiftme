<% if @recipients %>
  <% @recipients.each do |recipient| %>
    $('<%= "##{Desk.find(recipient.desk_id).abrev}.recipient_desk.on" %>').removeClass('<%= "#{recipient.id}" %>').addClass("off").removeClass("on"); 
  <% end %>
<% end %>
