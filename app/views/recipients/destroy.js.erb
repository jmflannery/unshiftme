<% if @recipients %>
  <% @recipients.each do |recipient| %>
    $('<%= "##{Workstation.find(recipient.workstation_id).abrev}.recipient_workstation.on" %>').removeClass('<%= "#{recipient.id}" %>').addClass("off").removeClass("on"); 
  <% end %>
<% end %>
