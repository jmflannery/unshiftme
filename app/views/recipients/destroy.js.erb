<% if @recipients %>
  <% @recipients.each do |recipient| %>
    var el = $('<%= "##{Workstation.find(recipient.workstation_id).abrev}.recipient_workstation.on" %>').turnOff().removeData('recipient_id'); 
  <% end %>
<% end %>
