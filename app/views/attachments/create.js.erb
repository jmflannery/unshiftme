// clear the file upload form field
$('input[type="file"]').val("");

// hide the file upload form fields
$('#upload_section').hide();
$('#messages_section').css("height", "+=45");

// display the new message 
display_new_message("<%= j(render(@message)) %>");

